# 原始文档

本目录保存 SDK 重写、解释或实现工作之前的原始输入文档，用于保留项目基线。

## 规则

- 本目录中的项目文档是原始资料。
- Agent 不允许私自新增、移动、重写、原地总结或修改本目录中的文件。
- 只有在用户明确要求某个具体变更时，agent 才能修改本目录。
- 派生 SDK 文档、设计说明、评审记录和实现计划必须放在 `docs/SDK/` 或其它非 `origin` 目录下。

## 当前文件

- `dev_hardware_sketch.md`
- `endpoint_peripheral_validation_board.md`
