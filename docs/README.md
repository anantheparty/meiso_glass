# 文档索引

`docs/` 顶层只保留三个入口：

- [`origin/`](origin/)：原始输入文档。这里保存项目早期基线资料，不允许 agent 私自新增、移动、重写或修改。
- [`SDK/`](SDK/)：SDK 派生文档。这里只保存三份核心设计文档和索引。
- [`validation/`](validation/)：真机验证计划和实验记录。这里保存刷机、bring-up、链路验证和测量设计，不作为 SDK 使用指南。

除 `origin/` 内明确要求保留原貌的原始文档外，解释性文档正文统一使用中文，目录名和文件名使用英文。
