device:
  id: imx8mm-endpoint-001
  role: endpoint
  platform: imx8mm

network:
  bind_host: 0.0.0.0
  control_port: 42001
  peer_host: 192.168.10.3
  peer_heartbeat_port: 42000

video:
  # 仅用于上电调试。探测 BSP 专用 VPU 元素后再替换编码器。
  host: 192.168.10.3
  port: 5000
  width: 1280
  height: 720
  fps: 30
  encoder: x264enc

logging:
  dir: ./logs
